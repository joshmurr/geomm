{
  "name": "@example/triangle-strip",
  "version": "0.0.1",
  "private": true,
  "description": "TODO",
  "repository": "https://github.com/joshmurr/geomm",
  "author": "Josh Murr",
  "scripts": {
    "start": "vite --open",
    "build": "tsc && vite build --base='./'",
    "preview": "vite preview --host --open"
  },
  "devDependencies": {
    "typescript": "^4.9.4",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "@geomm/api": "*",
    "@geomm/maths": "*",
    "@geomm/core": "*",
    "@geomm/webgl": "*",
    "@geomm/dom": "*",
    "@geomm/geometry": "*",
    "gl-matrix": "^3.4.0"
  },
  "browser": {
    "process": false
  }
}
